---to build images
docker build -t my-flask-app .

---if you have different docker file name then execute this
docker build -t my-flask-app -f YourDockerfile .


---to run images
docker run -p 4000:5000 my-flask-app

[NOTE: here the 4000 is the host side and the 5000 is the container side so we have to enable to host port on security groups.]


---to check the the conatiners is working or not execute this in the browser
http://13.234.31.1:4000/
---for java based use this
http://13.234.31.1:4000/codename/

